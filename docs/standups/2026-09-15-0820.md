# Stand-up 2026-09-15 08:20 — Combined trip total surface

Phase change: **Build → Verify** (steps 3 → 4 of `docs/team-protocol.md`). Branch `feat/combined-trip-total`, nothing committed.

| Who | Done | Next | Blockers |
|---|---|---|---|
| pm | Respawned with `SendMessage` (verified). Read spec rev 4.1 + stand-up. Took two A1 items from the lead: copy added beyond 7.9, and the Flights-tab honest figure moving to the audited all-in | A1 after V1 | Waits on V1 |
| backend | R1 closed (endpoint fix in rev 2). No code this slice | Idle; follow-ups F2, F7 | None |
| frontend | FE1 `src/services/tripTotal.ts` + `formatCents`/`formatShortDate` in `flightFormat.ts`. FE2 `src/components/TripTotalCard.tsx` + `.css`. FE3 `src/SpotDetailPage.tsx` + `.css`: pick buttons, teaser honest → `estimatedEntryPrice`, cents + `formatShortDate` on teaser/stay rows, card outside tab panels; Getting there rows and `formatPrice` untouched. tsc 0, eslint 0 new, 111 related unit tests, copyStandards green. Browser check of 24–31 at 1280/400 px; T3 13/13 on :3001 (this checkout) | Idle; fix any BUG from V1 | None |
| sdet | T1 55/55 (C1–13, 34). T2 23/23 (C14–23). T3 written (C24–32). V1 so far: typecheck 0; `npm test` 66 files / 809 pass; build 0 | Finish V1: T3 on :3002 from this checkout; lint breakdown; per-criterion report to pm + lead | Lint exit 1 (120 `no-undef 'process'` errors), under investigation |

## Tasks

| ID | Owner | Status |
|---|---|---|
| R1 | backend | done |
| FE1 | frontend | done |
| FE2 | frontend | done |
| FE3 | frontend | done |
| T1 | sdet | done (55/55) |
| T2 | sdet | done (23/23) |
| T3 | sdet | written; frontend's run 13/13 on :3001; sdet's official run pending |
| V1 | sdet | done, fully green: 34/34. T3 13/13 on :3002 (this checkout); tests 809 ✅; typecheck ✅; build ✅; lint ✅ after FE4 (0 errors, 43 warnings) |
| FE4 | frontend | done: `.claude/**` in `eslint.config.js` ignores; `npm run lint` exit 0 (0 errors, 43 warnings); HANDOFF to sdet |
| A1 | pm | done: accepted 34/34 (see 2026-09-15-0827 wrap-up) |

## Acceptance criteria (per sdet, pre-V1)
- 1–13, 34: passing (T1).
- 14–23: passing (T2).
- 24–32: T3 written. The earlier 3/13 run went against the wrong server (:3000 = F11 worktree) and is discarded. frontend reports 13/13 against this checkout; the official V1 run is pending.
- 33 (gates): typecheck, test and build pass; lint failing (cause under investigation).

## Decisions since last stand-up
- `pm` respawned with `SendMessage`; no more relaying.
- V1 must run against a dev server started from `/Users/vaimar/src/apps/webagency`. `:3000` belongs to the F11 worktree and must not be stopped or reused.
- pm will judge frontend's extra copy ("Flight out" / "Stay" headings, unparseable-fare message) at A1 and record it in 7.9 if acceptable.
- The Flights-tab honest figure moving to the audited all-in (e.g. DUB→MRS €51.99 → €66.99) is the intended rev 2 decision; no user call needed.

## Risks and asks for the user
- **Lint gate red.** Suspected cause: `eslint .` also lints `.claude/worktrees/*` (other sessions' checkouts). If confirmed, this is a local-environment issue and not in the branch; the lead decides whether to add an ignore. If any error is in branch files, it goes back to frontend as a BUG.
- **Merge risk:** FE3 and the F11 worktree both edit `SpotDetailPage.tsx`.
- No questions for the user.

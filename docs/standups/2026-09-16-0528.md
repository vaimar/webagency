# Stand-up 2026-09-16 05:28 — Train ways in to French spots (SNCF)

Phase change: **Spec → Contract review** (steps 1 → 2 of `docs/team-protocol.md`).
Spec: `docs/specs/sncf-rail-ways-in.md` (rev 1, 39 criteria + 40–42 conditional).

**Interruption:** a session rate limit ended all four teammates mid-review late on 2026-09-15. None was resumable ("no agent reachable"), so the lead respawned the whole team at 05:28 on 2026-09-16. No code was written and no files were lost. Only `pm` answered the STANDUP before the limit hit; the other three rows below come from their orientation reports.

| Who | Done | Next | Blockers |
|---|---|---|---|
| pm | Wrote the spec, rev 1: `GET /api/spots/{slug}/rail` with optional `arrivalAirport` + `arrivalTime`, nine statuses with fixed copy, no price (fare null, `priceState` MANUAL_CHECK, "Check fares on SNCF Connect" link to the home page — no documented prefill URL), caching (station 30 d, no-station 7 d, journeys 24 h), 1000/day cap, task split R0 / BE1–BE4 / FE1–FE4 / T1–T3 / V1 / A1 | Rev 2 with the user's four answers, then run the review | Was waiting on three review replies when the limit hit |
| backend | Orientation + 4 live probe calls. **SNCF returns no fares** (found=false, total "0.0"); journeys are station-to-station only (coordinates 404); `places_nearby` returns halts, not mainline stations; journeys carry trains, times, changes, CO2, per-section GeoJSON. Wiring precedent: AviationStackScheduleClient + ApiUsageCounter | Contract review | None |
| frontend | Orientation: Getting there tab renders `spot.access[]`, MODE_ICON has TRAIN, `AccessFare` is fare-first, unpriced TRAIN shows "curated, no live price"; only 2 French spots have a curated train way; no SNCF code in the frontend | Contract review | None |
| sdet | Orientation: stub patterns (Xotelo interceptor, AviationStack quota counter, standalone MockMvc); `SpotAccessPricingServiceTest::leavesFerryAndTrainWaysUnpriced` may be invalidated; `SpotArrivalServiceTest` is the precedent for provider-down | Testability review | None |

## Tasks
| ID | Task | Owner | Status |
|---|---|---|---|
| — | Spec rev 2 (user decisions) | pm | in progress |
| R0 | Contract review | backend, frontend, sdet | in progress |
| BE1–BE4 | Navitia client, station resolution, endpoint, caching/quota | backend | not created |
| FE1–FE4 | Rail block on the spot page | frontend | not created |
| T1–T3, V1 | Tests and verification | sdet | not created |
| A1 | Acceptance | pm | not created |

## Acceptance criteria
1–39: not started. 40–42 (map trace) are out of scope by user decision.

## Decisions since last stand-up
- **User:** SNCF is for routing, not pricing — "flight and then take train… take this line". Show the train brand and number, stations, times, duration and changes.
- **User:** the price is a "Check fares on SNCF Connect" link; never an amount, never €0; excluded from every total.
- **User:** connection buffers are more generous than rev 1 — CDG 90, LYS 75, ORY 150, BVA 180 minutes.
- **User:** drawing the route on the map is the first follow-up, not this slice.
- **User:** SNCF API terms are fine for the private beta; the lead adds the SNCF API to LAUNCH-CHECKLIST item 9 before any public launch.
- **User:** sample origins stay CDG + Paris, LYS when near, on a labelled sample date.

## Risks and asks for the user
- No open questions; all four spec questions are answered.
- Session rate limits can end teammates outright, not pause them. Work must stay in files (spec, stand-ups) so a respawned team loses nothing.
- Backend build work needs its own slumber worktree: the main checkout holds another session's uncommitted island-hop changes.
- **Lead to do:** add the SNCF API to `docs/LAUNCH-CHECKLIST.md` item 9.

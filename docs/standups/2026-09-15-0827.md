# Stand-up 2026-09-15 08:27 — Combined trip total surface (wrap-up)

Phase change: **Accept → Wrap-up** (steps 5 → 6 of `docs/team-protocol.md`). **Feature accepted, 34/34.**
Branch `feat/combined-trip-total` (from `main` @ bff4007). Nothing committed or pushed.

| Who | Done | Next | Blockers |
|---|---|---|---|
| pm | A1: all 34 active criteria accepted (#35 withdrawn), checked against code and tests. Spec → rev 4.2, recording the built additions (line headings, unusable-fare copy, coordinates-only render, picks reset on slug change, no-airport exclusion text) in 7.7–7.9. Status line set to accepted | None in this slice | None. SendMessage failed mid-run ("disabled for this session"), and a rate limit ended the last turn after the status edit landed |
| backend | R1 contract review (endpoint fix) | Follow-ups F2, F7 | None |
| frontend | FE1 `tripTotal.ts` + `formatCents`/`formatShortDate`; FE2 `TripTotalCard`; FE3 `SpotDetailPage` wiring; FE4 `.claude/**` ESLint ignore | None | None |
| sdet | T1 55/55, T2 23/23, T3 13/13; V1 fully green | None | None |

## Tasks
All done: R1, FE1–FE4, T1–T3, V1, A1.

## Acceptance criteria
1–34: **accepted** (#35 withdrawn).
- 1–13, 34: `src/services/tripTotal.test.ts`, `src/services/flightFormat.test.ts`
- 14–23: `src/components/TripTotalCard.test.tsx`
- 24–32: `cypress/e2e/spot-trip-total.cy.ts` (13/13 against this checkout on :3002)
- 33 gates: `npm run lint` 0 errors (43 existing warnings), `npm run typecheck` 0, `npm test` 66 files / 809 tests, `npm run build` 0

## Change set (uncommitted)
- New: `src/services/tripTotal.ts` (+ test), `src/components/TripTotalCard.tsx` / `.css` (+ test), `cypress/e2e/spot-trip-total.cy.ts`, `cypress/fixtures/trip-total/`, `docs/specs/combined-trip-total.md`, `docs/standups/`
- Modified: `src/SpotDetailPage.tsx` / `.css`, `src/services/flightFormat.ts` (+ test), `eslint.config.js` (`.claude/**` ignore), `AGENTS.md` (F9), `.claude/agents/product-manager.md` (+ SendMessage)

## Decisions since last stand-up
- C33: lead chose option (a), ignoring `.claude/**` in ESLint (nested Claude worktrees), over accepting a red gate.
- pm accepted frontend's extra copy and the Flights-tab honest figure moving to the audited all-in (e.g. DUB→MRS €51.99 → €66.99), which is the rev 2 decision.

## Follow-ups (from spec section 5)
- **F1** return flight pick · **F3** opt-in riding line: the next product slices
- **F2** dated `/api/hotels/curated` · **F7** `POST /api/trips/total`: backend
- **F4** persist picks / feed from cart · **F5** one totals core · **F8** FX
- **F11** Getting there fare-first: running in another worktree · **F13** Getting there rows to cents: after this lands and F11 merges
- **F12** flight cart and Trip Ledger still whole euros (known difference)
- **F6** invented prices: island-hop part running in another worktree; TripGuide `estimatePackage()` still open

## Risks and asks for the user
- **Commit / PR:** nothing is committed. Needs the user's go-ahead.
- **Merge:** FE3 and the F11 worktree both edit `src/SpotDetailPage.tsx`; expect a conflict when the second one merges.
- Cypress (T3) isn't in CI, so the e2e criteria are only proven locally.

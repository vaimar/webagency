# Stand-up 2026-09-15 07:57 — Combined trip total surface

Phase change: **Contract review → Build in parallel** (steps 2 → 3 of `docs/team-protocol.md`).

| Who | Done | Next | Blockers |
|---|---|---|---|
| pm | *No STANDUP reply (it arrived while pm was finishing the review; row filled from pm's last report).* Spec rev 1 → 4.1: backend's endpoint fix (rev 2), frontend's 7 fixes + 3 clarifications (rev 2), sdet's mocks/criteria (rev 3), user decisions (rev 4), sdet's date-label + request-scope fixes (rev 4.1). F13 recorded as decided | Close-out spec edit (status "agreed", frontend's two FE3 notes, T1 date-only case); then A1 after V1 | No SendMessage tool; the lead relays pm's messages |
| backend | R1: OBJECT on the endpoint (`GET /api/flights`, not `/refresh`; fixed in rev 2), AGREE on points 1–4. Caveats: UK Ryanair fares come back in GBP (safe, since non-EUR lines are excluded); the curated check-in is 36–42 days out | Nothing: no build task. Follow-ups F2 (dates on `/curated`), F7 (`POST /api/trips/total`) | None. R1 closed (backend's stand-up asked pm to confirm the fix; it's in rev 2) |
| frontend | rev 1 OBJECT (7 fixes, taken); rev 4 AGREE (`formatCents` checked against #34, V1, V2; no spot test asserts whole euros); rev 4.1 OK. No code yet | FE1 → FE2 → FE3 | None |
| sdet | rev 1 OBJECT (endpoint, T3 mocks, wording); rev 4 OBJECT (C13 date label, C30 scope); rev 4.1 AGREE (`formatShortDate` in 4 time zones, `formatCents` vectors, `<output>` = role status). No tests yet | T1 → T2 → T3, then V1 | Needs frontend HANDOFFs for V1 |

## Tasks (no task tool in this session, tracked here)

| ID | Task | Owner | Depends on | Criteria | Status |
|---|---|---|---|---|---|
| R1 | Contract review | backend | — | 4, 6, 8 | done |
| FE1 | `src/services/tripTotal.ts` + `formatCents` / `formatShortDate` in `flightFormat.ts` | frontend | — | 1–13, 34 | done (HANDOFF to sdet) |
| FE2 | `src/components/TripTotalCard.tsx` + CSS | frontend | contract | 14–23, 31 | done (HANDOFF to sdet; #31 viewport checked in browser with FE3) |
| FE3 | Wire into `src/SpotDetailPage.tsx` (teaser row date → `formatShortDate`; Getting there rows L1092–1101 and `formatPrice` untouched) | frontend | FE1, FE2 | 24–32 | done (HANDOFF to sdet; 24–31 hand-checked at 1280/400 px on :3001) |
| T1 | `src/services/tripTotal.test.ts` + cases in `flightFormat.test.ts` | sdet | contract | 1–13, 34 | done, green vs FE1 (55/55) |
| T2 | `src/components/TripTotalCard.test.tsx` | sdet | contract | 14–23 | done, green vs FE2 (23/23) |
| T3 | `cypress/e2e/spot-trip-total.cy.ts` | sdet | contract; green after FE3 | 24–32 | written; 3/13 pass on pre-FE3 code (10 wait on pick buttons, expected) |
| V1 | Verify per criterion + gates (#33) | sdet | FE1–FE3, T1–T3 | 1–34 | blocked on build |
| A1 | Acceptance check | pm | V1 | all | blocked on V1 |

Build branch: `feat/combined-trip-total` (created from `main` @ bff4007 in the main checkout). No commits yet.

## Acceptance criteria
1–34: not started (35 withdrawn). The contract is agreed; tests and code are being written in parallel.

## Decisions since last stand-up
- **User:** headline = fare + stay, with the all-in adjacent (as the spec proposed).
- **User:** return flight and riding are excluded and listed under "Not in this total"; F1 and F3 are next.
- **User:** the card shows **cents** (`≈ €549.65`), via a new `formatCents()`. `euro()` and the flight cart stay whole euros (known difference, F12).
- **User (F13):** Getting there rows move to cents too, as a follow-up after this lands and F11 merges.
- The picked teaser row and stay row switch to `formatCents`, so row text matches card text (#27).
- The flight label date is built by hand (`formatShortDate`), not `toLocaleDateString` (ICU differences on Node 25).
- The #30 no-request check counts only `/api/**` and `/actuator/**` (MapLibre tile fetches).
- Picks carry a stable id (FlightTeaser unmounts on tab switch).
- F9 done: `AGENTS.md` open items for the trip total and the activity cost gap corrected.
- pm respawned after this stand-up with `SendMessage` added to `.claude/agents/product-manager.md` (the old role had none). Verified: the new pm messaged the lead directly. sdet sends the V1 report to pm and the lead; no more relaying.

## Risks and asks for the user
- No asks: every product question is answered.
- **Merge risk:** F11 (Getting there fare-first) is being built in worktree `claude/laughing-pasteur-6d7c4f` or `claude/happy-rosalind-5b881e`, and edits `SpotDetailPage.tsx`, which FE3 also edits. Expect a conflict at integration; the lead handles it.
- Island-hop hardcoded-rate removal (part of F6) is in the other worktree session; not tracked by this team.
- #31 (400px layout) and T3 run in Cypress only, and Cypress isn't in CI.

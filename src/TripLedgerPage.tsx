import TripLedger from './TripLedger';

// Route shell for /trip-ledger — the door-to-door multi-stop trip cost model
// (drive + park + fly + road-trip + stays + activities in one honest total).
export default function TripLedgerPage() {
    return <TripLedger />;
}

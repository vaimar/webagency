import HackFlights from './HackFlights';

// Route shell for /hack-flights — the standalone Self-Transfer / creative
// routing search. Deliberately bypasses the multi-accommodation explore
// dashboard: flights and routing only.
export default function HackFlightsPage() {
    return <HackFlights />;
}
